CREATE TABLE public."Activity" ( "id" TEXT NOT NULL, "userId" UUID NOT NULL, "action" TEXT NOT NULL, "target" TEXT NOT NULL, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "Activity_pkey" PRIMARY KEY ("id") ); ALTER TABLE public."Activity" ADD CONSTRAINT "Activity_userId_fkey" FOREIGN KEY ("userId") REFERENCES public."profiles"("id") ON DELETE CASCADE ON UPDATE CASCADE; CREATE INDEX "Activity_userId_idx" ON public."Activity"("userId"); CREATE INDEX "Activity_createdAt_idx" ON public."Activity"("createdAt");
